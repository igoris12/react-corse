import { createContext } from 'react';
export const Context = createContext('Default Value');
export const workContext = createContext('Default Value');
export const CountState = createContext(null);
